import React from 'react';
import Hero from '../components/Hero';

const Rooms = () => {
    return (
        <Hero hero="roomsHero">
            <div>Rooms</div>
        </Hero>
    );
};

export default Rooms;
